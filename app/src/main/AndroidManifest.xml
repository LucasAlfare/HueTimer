<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bomesmo.huetimer.main">

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_custom"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".fazendo_de_novo_recyclerview.MainActivity2"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.StatisticsActivity">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.bomesmo.huetimer.main.activities.MainActivity" />
        </activity>
        <activity android:name=".activities.TimesListActivity">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.bomesmo.huetimer.main.activities.MainActivity" />
        </activity>
        <activity android:name=".activities.AboutActivity">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.bomesmo.huetimer.main.activities.MainActivity" />
        </activity>
        <activity android:name=".activities.ViewTestActivity" />
        <activity android:name=".activities.fewst_moves_helper.FewestMovesHelperActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.bomesmo.huetimer.main.activities.MainActivity" />
        </activity>
        <activity android:name=".activities.square1_csp_trainer.CspTrainerActivity" />
        <!-- <activity -->
        <!-- android:name=".fazendo_de_novo.MainActivity2" -->
        <!-- android:label="@string/title_activity_main2" -->
        <!-- android:theme="@style/AppTheme.NoActionBar"></activity> -->
    </application>

</manifest>